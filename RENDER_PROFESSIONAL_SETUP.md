# Render Professional Deployment Setup

## ✅ Configuration Updated for Professional Plan

### Changes Made:
1. **Plan**: Updated from `starter` to `professional`
2. **Instance Type**: Set to `professional`
3. **Region**: Set to `oman` (closest to your location)
4. **Persistent Disk**: Added 10GB disk for database and uploads
5. **Auto Deploy**: Enabled automatic deployments

## 🚀 Deployment Steps

### 1. Connect Repository to Render
1. Go to [Render Dashboard](https://dashboard.render.com)
2. Click "New +" → "Web Service"
3. Connect your GitHub account
4. Select repository: `fuhoodkh/Drone-Initiative`
5. Render will auto-detect `render.yaml` configuration

### 2. Verify Configuration
- **Root Directory**: `server`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Plan**: Professional
- **Region**: Oman

### 3. Environment Variables
Set in Render Dashboard → Environment:
- `NODE_ENV` = `production`
- `JWT_SECRET` = (Render will auto-generate, or set your own)
- `PORT` = `3000` (Render sets this automatically)

### 4. Persistent Disk
- **Name**: `drone-initiative-disk`
- **Size**: 10GB
- **Mount Path**: `/opt/render/project/src/server/data`
- **Purpose**: Store SQLite database and uploaded files persistently

## 📊 Professional Plan Benefits

### What You Get:
- ✅ **Persistent Storage**: Database and files won't be lost
- ✅ **Better Performance**: Professional instance type
- ✅ **No Cold Starts**: Always-on service
- ✅ **Higher Limits**: More resources and bandwidth
- ✅ **Better Support**: Priority support
- ✅ **Custom Domain**: Add your own domain
- ✅ **SSL Certificate**: Automatic HTTPS

## 🔧 Database Storage

With the persistent disk:
- Database: `/opt/render/project/src/server/data/platform.db`
- Uploads: `/opt/render/project/src/server/uploads/`
- **Data persists** across deployments and restarts

## 📝 Next Steps

1. **Deploy on Render**:
   - Render will automatically detect `render.yaml`
   - The service will be created with professional settings

2. **Initialize Users**:
   ```bash
   # After first deployment, SSH into the service
   cd /opt/render/project/src/server
   node init-users.js
   ```

3. **Test the Deployment**:
   - Health check: `https://your-service.onrender.com/api/health`
   - Main app: `https://your-service.onrender.com/`
   - Login: `admin` / `admin123`

## 🔐 Security Notes

- JWT_SECRET is auto-generated by Render (secure)
- Database is stored on persistent disk (survives restarts)
- All traffic is encrypted with HTTPS
- Consider setting up a custom domain

## 📞 Support

If you encounter issues:
1. Check Render logs in the dashboard
2. Verify environment variables are set
3. Check that the persistent disk is mounted correctly
4. Contact Render support (Professional plan includes priority support)
